apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: mysecret
  namespace: mysecret-appsub
spec:
  channel: mysecret-channel/mysecret
  placement:
    placementRef:
      name: mysecret
      kind: PlacementRule
