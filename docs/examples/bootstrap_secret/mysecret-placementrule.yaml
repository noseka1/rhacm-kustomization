apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: mysecret
  namespace: mysecret-appsub
spec:
  clusterReplicas: 1
  clusterSelector:
    matchLabels:
      name: mycluster
